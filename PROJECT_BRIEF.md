# COCKPIT (가칭) — 프로젝트 설계 문서

> AI가 짠 코드를 **개발자가 직접** 검증하기 쉽게 만드는 로컬 단일 화면 도구. 근거 수집은 전부 AI 없이 계산한다.

작성일: 2026-09-09
상태: 설계 확정, M0 착수 전

---

## 0. 핵심 주장 (이 문서의 전부)

> **"AI가 짜준 게 의심스러워서 검사를 하는데, 그걸 또 AI가 검사한다고?"**

현재 AI 코드 검증 시장은 전부 LLM 기반이다. CodeRabbit, Greptile, Qodo, Cursor BugBot 모두 PR 시점에 LLM을 호출해 코드를 읽고 의견을 낸다. 그 결과:

- **비싸다** — 개발자당 월 $24~40, 리뷰 횟수 초과분 과금
- **느리다** — PR이 열린 뒤에야 동작
- **비결정적** — 같은 코드에 매번 다른 결과
- **논리적으로 이상하다** — 못 믿는 대상을 같은 종류의 것으로 검증한다

### 그 허점 사이로 개발자가 들어간다

COCKPIT은 "AI 대신 우리가 더 잘 검증한다"가 아니다. **검증의 주체는 개발자다.**

AI가 만든 것을 AI가 검증하는 고리 사이에 사람이 끼어들어야 하는데, 지금은 그게 너무 비싸다. diff는 2.5배로 커졌고, 에이전트는 완성된 결과만 던져서 의도는 사라졌고, 판단에 필요한 근거는 네 개의 다른 도구에 흩어져 있다. 그래서 개발자는 결국 대충 승인하거나, 또 다른 AI에게 떠넘긴다.

**COCKPIT이 하는 일은 판정을 대신 내리는 게 아니라, 개발자가 직접 판단하는 비용을 낮추는 것이다.**

| 기계가 하는 일 | 사람이 하는 일 |
|---|---|
| 사실 수집 — 무엇이 바뀌었나, 어디까지 영향이 가나, 무엇이 중복인가, 주장이 실제와 맞나 | 판단 — merge할지, 되돌릴지, 다시 시킬지 |

이 구분이 흐려지는 순간 제품은 또 하나의 자동 리뷰 봇이 되고, 위에서 지적한 바로 그 고리 안으로 들어간다. 그래서 자동 수정을 비목표에 넣었고, 모든 판정에 클릭 가능한 근거를 붙인다.

그리고 그 사실 수집은 **AST·그래프·해시 기반의 결정론적 계산으로만 한다. 분석 경로에 LLM 호출이 단 한 번도 없다.**

그래서 얻는 것:
- 무료 (토큰 비용 0)
- 즉시 (밀리초 단위)
- 재현 가능 (CI에 그대로 투입 가능)
- 오프라인 (코드가 기기 밖으로 안 나감)

이건 기능 목록이 아니라 **포지션**이다. 모든 설계 결정은 이 주장을 배신하지 않는 선에서 이뤄진다.

---

## 1. 제품 정의

개발자가 여러 모니터 중 **한 화면을 통째로 내주고**, AI 에이전트가 코드를 짜는 동안 켜놓는 로컬 대시보드.

- **대상**: 개인 개발자, 소규모 팀, OSS 메인테이너
- **배포**: v1은 **CLI 우선**(`pipx install cockpit`). 도커 이미지는 CLI가 자리 잡은 뒤 (§9의 마운트·감시 문제는 도커를 기본으로 골랐을 때만 발생한다)
- **OS**: Linux / macOS / Windows 전부 지원 (§9)
- **라이선스**: 오픈소스 (MIT 또는 Apache-2.0)
- **화면**: 전체화면 1개로 검토가 완결된다. 터미널 포함이라 여기서 나갈 일이 없다

```bash
docker run -v $PWD:/repo -p 7777:7777 -p 4318:4318 cockpit
# 7777 = UI + 이벤트 수신, 4318 = OTLP 수신(토큰/비용)
# → localhost:7777 열면 끝. 설정 파일 없음.
```

### 비목표 (Non-goals)

명시적으로 하지 않는 것. 요청이 와도 거절한다.

- ❌ 클라우드 SaaS, 계정, 로그인
- ❌ 분석 경로에서의 LLM 호출 (선택적 "설명 생성" 기능조차 v1에서는 안 넣는다)
- ❌ 코드 자동 수정 (판정만 한다. 고치는 건 에이전트 몫)
- ❌ 자체 터미널 에뮬레이터 구현 (xterm.js + PTY 브리지 사용)
- ❌ 팀 협업 기능, 권한 관리, 대시보드 공유

---

## 2. 사용 시나리오

1. 개발자가 Claude Code로 작업을 시킨다
2. 두 번째 모니터의 COCKPIT이 **실시간으로** 반응한다
   - 지금 어떤 파일을 읽고 어떤 도구를 부르는지
   - 코드베이스 구조에서 어디가 바뀌는지, 영향 반경이 어디까지인지
   - 방금 추가된 코드에 중복·에러 마스킹·가짜 테스트가 있는지
   - 에이전트가 "테스트 통과했습니다"라고 하면, 실제로 통과했는지
3. 개발자는 화면만 보고 **merge할지 되돌릴지** 판단한다
4. 판단 근거가 필요하면 리뷰 패킷을 마크다운으로 뽑아 PR 본문에 붙인다

---

## 3. 아키텍처

```
[수집기 Collectors]         [코어 데몬 Core]                    [뷰 View]

 에이전트 훅 어댑터 ┐
 Git 워처          ├→ 이벤트 버스 → 분석기 레지스트리 → 스냅샷 스토어 → WS → 단일 화면
 파일시스템 워처    │    (링 버퍼)      (플러그인)          (SQLite)
 PTY 호스트        ┘
```

### 3.1 수집기

각 수집기는 "무엇이 일어났는지"만 보고한다. 판단하지 않는다.

| 수집기 | 소스 | 비고 |
|---|---|---|
| `agent/claude-code` | **HTTP 훅**으로 직접 POST (§10.1) | 셸 없이 동작 → OS 분기 0. 어댑터 경계는 반드시 분리 |
| `agent/cursor` | `afterFileEdit` 등, 수집기 바이너리 경유 (§10.2) | 변경 전/후 내용을 함께 주므로 파일 감시 불필요 |
| `agent/codex` | `.codex/hooks.json` + rollout JSONL (§10.3) | 세션 종료·서브에이전트 훅 없음 |
| `otlp` | Claude Code / Codex 텔레메트리 (§10.8) | 토큰·비용. 4318 포트 |
| `git` | libgit2 바인딩 또는 `git` CLI 파싱 | 커밋, 브랜치, diff, 2주 처른 판정 |
| `fs` | inotify / FSEvents (watchdog) | 200ms 디바운스. 에이전트는 파일을 연속으로 쓴다 |
| `pty` | PTY 프로세스 | 터미널 패널용 |

### 3.2 이벤트 버스

모든 수집기 출력이 하나의 타입 있는 스트림으로 정규화된다. **이게 프레임워크의 본체다.**

- 고정 크기 링 버퍼 (기본 50,000건) — 장시간 세션 메모리 상한 보장
- 링에서 밀려난 이벤트는 SQLite 추가 전용 로그로 흘려보냄
- 구독자는 offset 기반으로 재생 가능

### 3.3 분석기 (플러그인)

```python
class Analyzer(Protocol):
    id: str                    # "dup.block", "risk.error-masking"
    incremental: bool          # 증분 갱신 지원 여부

    # 입력은 파일 상태와 변경 집합뿐. 훅 이벤트는 들어오지 않는다 (§11)
    def analyze(self, change: ChangeSet, snap: Snapshot) -> list[Finding]: ...
```

`ChangeSet`은 훅에서도, 파일 감시에서도, `git diff`에서도 똑같이 만들어진다. 그래서 같은 분석기를 실시간 화면 · CI · 과거 커밋 대량 평가(§12)에 그대로 재사용할 수 있다.

**분석기는 순수 함수다.** I/O 없음, 전역 상태 없음, LLM 호출 없음.
입출력이 JSON 직렬화 가능해야 한다 → 나중에 병목 나는 분석기만 Rust로 빼서 서브프로세스로 교체 가능.

```python
@dataclass
class Finding:
    analyzer_id: str
    severity: Literal["block", "warn", "info"]   # 기본값은 보수적으로
    file: str
    span: tuple[int, int]        # 줄 범위
    symbol: str | None
    message: str
    evidence: dict               # 판정 근거 (해시, 매칭된 다른 위치 등)
```

`evidence` 필드가 중요하다. **LLM 의견이 아니라 기계적 근거를 보여주는 게 제품의 정체성이다.** "중복입니다"가 아니라 "이 5줄이 `src/a.py:42`, `src/b.py:88`과 해시 일치"라고 말해야 한다.

### 3.4 스냅샷 스토어

- SQLite 단일 파일 (`/repo/.cockpit/state.db`)
- 외부 의존성 추가 금지 — 도커 이미지 단순함이 배포 전략의 핵심
- 심볼 인덱스, 의존성 그래프, 판정 이력, 세션 로그

---

## 4. 이벤트 스키마 (M0에서 확정)

```jsonc
{
  "v": 1,                          // 스키마 버전. 반드시 넣는다
  "id": "01J8X...",                // ULID
  "ts": 1757400000.123,
  "source": "agent/claude-code",   // 수집기 id
  "session": "sess_abc",
  "type": "tool.pre",              // 아래 표 참조
  "seq": 1042,                     // 세션 내 순번
  "parent": "01J8W...",            // 중첩 관계 (스택 복원용)
  "payload": { }                   // type별 구조
}
```

| type | 발생 시점 | payload 주요 필드 |
|---|---|---|
| `session.start` | 세션 시작 | `cwd`, `agent_version` |
| `tool.pre` | 도구 실행 직전 | `tool`, `args` |
| `tool.post` | 도구 실행 직후 | `tool`, `result_summary`, `duration_ms` |
| `file.changed` | 파일 변경 감지 | `path`, `hash_before`, `hash_after` |
| `git.commit` | 커밋 | `sha`, `files`, `insertions`, `deletions` |
| `claim` | 에이전트의 자연어 주장 추출 | `text`, `kind` (`test_passed` 등) |
| `session.stop` | 세션 종료 | `reason` |

`claim` 이벤트가 EVIDENCE 패널의 입력이다. 주장 추출은 정규식/패턴 매칭으로 한다 (여기서도 LLM 안 쓴다).

---

## 5. 분석기 목록

### M2 구현 대상 (킬러 기능)

#### `dup.block` — 중복 블록
- 5줄 이상 연속 반복 (GitClear 정의와 동일하게)
- 롤링 해시(Rabin-Karp) + winnowing으로 지문 생성
- Union-Find로 클론 클래스 그룹핑
- **근거**: 2023→2026 중복 +81%, 파일 간 재사용 −35%

#### `risk.error-masking` — 에러 삼키기
- 빈 except / catch, 로깅 없는 catch, `except: pass`, `catch (e) {}`
- 광범위 예외 포착 (`except Exception`, `catch (Throwable)`)
- tree-sitter 쿼리로 언어별 정의
- **근거**: 에러 마스킹 catch 블록 +47%

### M4 이후

#### `test.authenticity` — 테스트 진위
가장 빈 칸이다. 리뷰 봇들은 diff를 읽지 테스트의 유효성은 안 본다.
- assertion 없는 테스트 함수
- 검증 대상 자체를 mock한 테스트 (mock 대상 == 테스트 대상)
- 항상 참인 단언 (`assert True`, `assert x == x`)
- 새 public 심볼에 대응 테스트 없음
- 확장: 경량 뮤테이션 테스팅

#### `dup.semantic` — 의미 중복 (Type-4)
- AST 정규화(식별자 익명화) → shingle → MinHash → LSH 버킷
- O(n²) 전수 비교 회피
- **근거**: AI 에이전트가 사람보다 Type-4 클론을 더 많이 만들고, 리뷰어는 이를 오히려 덜 지적함 (arXiv 2601.21276)

#### `arch.cycle` / `arch.layer` — 구조 붕괴
- Tarjan SCC로 순환 의존성
- 위상 정렬로 레이어 역참조 (domain → infra 등)

#### `churn.rework` — 조기 재작업
- 2주 내 되돌려지거나 재작성된 라인 비율

#### `claim.verify` — 주장 검증
- `claim` 이벤트를 실제 실행 결과와 대조
- "테스트 통과" 주장 → 해당 시점 테스트 실행 기록 존재 여부 + 종료 코드

---

## 6. 자료구조 · 알고리즘 매핑

성능 요구가 실제로 발생하는 지점만.

| 자료구조 / 알고리즘 | 적용 위치 | 목적 |
|---|---|---|
| 그래프 + 역방향 BFS (깊이 제한) | 심볼 의존성 그래프 | 영향 반경 "이거 고치면 뭐가 깨지나" |
| Tarjan SCC | 아키텍처 분석 | 순환 의존성 탐지 |
| 위상 정렬 | 아키텍처 분석 | 레이어 위반 탐지 |
| 스택 | 세션 컨텍스트 복원 | task→subtask→tool call 중첩. `tool.post`에서 pop |
| 링 버퍼 (고정 큐) | 이벤트 버스 | 메모리 상한 보장 |
| 우선순위 큐 | 분석기 스케줄러 | 영향 반경 큰 변경부터 |
| 디바운스 큐 | FS 워처 | 연속 쓰기 병합 |
| 롤링 해시 + winnowing | `dup.block` | O(n) 지문 생성 |
| MinHash + LSH | `dup.semantic` | 후보 축소, O(n²) 회피 |
| Union-Find | 클론 그룹핑 | 중복 쌍 → 클래스 |
| 인터벌 트리 | diff ↔ 심볼 매핑 | 어느 함수가 수정됐나, O(log n) |
| Merkle 해시 + LRU | 증분 재분석 | **성능의 90%가 여기서 결정된다** |
| Aho-Corasick | 시크릿/위험 패턴 | 수백 패턴 1패스 |
| tree-sitter 쿼리 | 구문 기반 판정 | 정규식으로 하면 안 되는 것들 |

> ⚠️ **증분 재분석은 처음부터 넣는다.** 전체 재인덱싱은 최초 1회. 이후는 Merkle 해시로 바뀐 파일만. 나중에 넣으려면 전부 갈아엎어야 한다.

---

## 7. 화면 구성

> ⚠️ **미확정.** 사용자 0명 상태의 UI 설계는 대개 틀린다. 아래는 최종 지향점이고, 실제로는 `EXECUTION_PLAN.md`대로 정적 웹 뷰 한 장에서 시작해 패널을 늘린다.


```
┌─────────────────────────┬──────────────────────────────┐
│  NOW                    │  DELTA                       │
│  이벤트 타임라인          │  심볼 그래프 + 변경 히트맵      │
│  현재 실행 스택           │  영향 반경 하이라이트           │
├─────────────────────────┼──────────────────────────────┤
│  RISK          ★킬러     │  EVIDENCE                    │
│  중복 / 에러마스킹        │  주장 vs 실제                 │
│  테스트진위 / 처른        │  "테스트 통과했습니다" → 검증    │
│  block / warn / info    │                              │
├─────────────────────────┴──────────────────────────────┤
│  TERMINAL (xterm.js + PTY)                             │
└────────────────────────────────────────────────────────┘
```

상단: 커맨드 팔레트 + **리뷰 패킷 내보내기** (세션 요약 + 판정 결과 → 마크다운)

### 패널 설계 규칙

1. **RISK가 전용 도구보다 확실히 나아야 한다.** 나머지 패널은 여기 온 사람을 붙잡는 역할이다. 통합 자체는 이유가 안 된다.
2. **판정은 3등급(block / warn / info), 기본값은 보수적으로.** 리뷰 봇 시장의 실패는 전부 정확도가 아니라 노이즈에서 났다. 경고가 많은 화면은 일주일이면 무시된다.
3. **모든 판정에 클릭 가능한 근거(evidence)가 붙는다.** 근거 없는 판정은 LLM 도구와 구별되지 않는다.
4. 뷰는 상태를 갖지 않는다. 전부 스냅샷 스토어의 투영이다.

---

## 8. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 데몬 | Python + FastAPI + WebSocket | 착수 속도. 분석기를 순수 함수로 격리해두면 병목만 나중에 교체 |
| 파싱 | tree-sitter (py-tree-sitter) | 다언어 확장이 파일 추가로 끝남 |
| 저장 | SQLite | 외부 의존성 0. 도커 이미지 단순함 유지 |
| 프런트 | React + WebSocket | |
| 그래프 렌더 | sigma.js 또는 Cytoscape (WebGL) | D3 직접은 수천 노드에서 죽음 |
| 터미널 | xterm.js + PTY 브리지 | 직접 구현 금지 |
| 패키징 | 멀티스테이지 도커 빌드 | 목표 300MB 이하 |

**언어 지원 순서**: Python → TypeScript → 나머지. 한 언어에서 깊게 먼저.

---

## 9. 크로스 플랫폼 요구사항

**필수 조건: Linux / macOS(Intel · Apple Silicon) / Windows 전부에서 동작한다.**

전략은 하나다. **분석 로직은 전부 컨테이너 안(리눅스 하나)에서 돌리고, OS 차이는 "호스트 수집기"라는 얇은 층 하나에 격리한다.**

```
[호스트 — OS별 차이가 여기에만 존재]        [컨테이너 — 리눅스 하나만 지원]

 Claude Code 훅 ─┐
 파일 감시        ├→ HTTP POST → localhost:7777 → 코어 데몬 + 분석기 + 웹 UI
 (host-collector)┘                                    ↑
                                          bind mount (읽기 전용)
```

- 호스트 수집기는 **의존성 없는 단일 실행 파일**이어야 한다. 파이썬이나 노드를 깔게 하는 순간 "docker run 한 줄" 약속이 깨진다. → **Go 권장** (크로스 컴파일 한 줄, 정적 링크, 세 OS 바이너리 자동 생성)
- 어차피 에이전트 훅은 호스트에서 발생한다. 파일 감시를 같은 자리에 두는 게 구조적으로도 맞다.
- 설치는 결국 2단계다(`docker run` + 수집기 바이너리). README에서 이걸 숨기지 말고, 수집기 없이도 폴링 폴백으로 동작한다는 점을 같이 적는다.

### 반드시 밟게 되는 지뢰 4개

1. **바인드 마운트 파일 감시가 macOS · Windows에서 안 된다.**
   inotify는 Docker Desktop의 VM 경계를 넘지 못한다. WSL2도 Windows 파일시스템 경로(`/mnt/c/...`)에서는 이벤트가 오지 않는다.
   → 감시는 컨테이너가 아니라 **호스트 수집기가 한다.** 컨테이너는 읽기만.
   → 수집기를 안 깐 사용자를 위해 git 기반 폴링 폴백(1~2초 간격)을 둔다.

2. **줄바꿈과 대소문자.**
   CRLF/LF 차이로 같은 코드가 다른 해시를 갖는다 → **중복 검출 결과가 OS마다 달라진다.** 결정론을 내세우는 제품에서 이건 치명적이다. 해시 계산 전 줄바꿈 정규화는 선택이 아니라 필수.
   macOS(APFS 기본)와 Windows는 대소문자 비구분이라, 경로를 키로 쓰는 모든 곳에서 정규화가 필요하다.

3. **터미널의 의미가 OS마다 다르다.**
   컨테이너 안의 PTY는 **사용자의 개발 셸이 아니라 컨테이너 셸**이다. v1은 컨테이너 셸로 가고 README에 명시한다. 호스트 셸 연결은 M5 이후.
   Windows는 ConPTY라 유닉스 PTY와 인터페이스가 다르다. 호스트 셸을 붙이는 순간 여기서 OS별 분기가 생긴다. 미루는 게 맞다.

4. **성능.**
   macOS Docker Desktop의 바인드 마운트 I/O는 느리다. 10만 줄 첫 인덱싱 10초 목표가 여기서 깨질 수 있다.
   → 인덱스 캐시를 명명 볼륨에 두고, 마운트 전체 훑기는 최초 1회로 끝낸다.

### 이미지

- `linux/amd64` + `linux/arm64` 멀티아치. Apple Silicon 때문에 필수. buildx로 빌드.
- UI는 브라우저라 OS 무관. tree-sitter 등 네이티브 의존성도 컨테이너 안에 있으므로 OS별 휠 문제가 사라진다. 이게 "분석은 전부 컨테이너" 전략의 가장 큰 이득이다.

### 검증

**M3 완료 기준에 세 OS 실기 확인을 포함한다.** Linux 네이티브, macOS(Docker Desktop, Apple Silicon), Windows(WSL2) 세 곳에서 실제로 동작하는 걸 확인하기 전에는 공개하지 않는다. "아마 되겠지"로 공개하면 첫 이슈가 전부 설치 실패로 채워진다.

---

## 10. 에이전트 연결: 후킹과 데이터 수집

로컬에서 AI를 쓰는 방식은 CLI · IDE · 데스크톱 앱 · 웹으로 갈린다. 도구마다 지원 수준이 다르므로 **3계층 폴백**으로 설계한다.

| 계층 | 방식 | 얻는 것 | 대상 |
|---|---|---|---|
| **L1** | 네이티브 훅 | 정확 · 실시간 · 의도 포함 | Claude Code, Cursor, Codex |
| **L2** | OTLP 텔레메트리 | 토큰 · 비용 · 세션 통계 | Claude Code, Codex |
| **L3** | git + 파일시스템 감시 | 무엇이 바뀌었는지만 | 나머지 전부 |

> **L3만으로도 제품이 성립해야 한다.** 웹 채팅에서 코드를 받아 붙여넣는 사용자도, Copilot 사용자도 RISK·DELTA 패널은 그대로 쓸 수 있어야 한다. L1은 EVIDENCE 패널과 의도 추적을 얹는 보너스다. 이 순서를 뒤집으면 특정 도구 종속 제품이 된다.

### 10.1 Claude Code (L1 — 지원 수준 최상)

- **설정 위치**: `~/.claude/settings.json`(사용자) · `.claude/settings.json`(프로젝트) · `.claude/settings.local.json` · managed settings
- **이벤트**: 30개 규모로 계속 늘어나는 중. 우리가 쓸 것 — `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `PostToolUseFailure`, `Stop`, `SessionEnd`, `PreCompact`/`PostCompact`, `FileChanged`
- **matcher**: `PreToolUse`/`PostToolUse`는 도구 이름(`Edit|Write`, `mcp__.*`), `SessionStart`는 `startup|resume|clear|compact`. `*`나 생략은 전부 매치. `UserPromptSubmit`·`Stop` 등은 matcher 없이 항상 발화
- **종료 코드**: 0 = 통과, 2 = 차단(stderr가 에이전트에게 전달). **exit 1은 차단이 아니라 soft failure로 취급되어 그냥 진행된다** — 차단은 반드시 2
- **★ HTTP 훅을 지원한다.** 커맨드 훅은 stdin으로 JSON을 받지만, HTTP 훅은 POST 본문으로 받는다.
  → **셸 스크립트 없이 `http://127.0.0.1:7777/events`로 직접 POST할 수 있다. 이게 크로스 플랫폼 관점에서 가장 큰 이점이고, 우리 설계의 기본 경로다.** OS별 셸 분기가 통째로 사라진다.
- **transcript_path**가 payload에 들어온다 → 전체 대화 JSONL에 접근 가능 (토큰 폴백 경로)
- ⚠️ **함정**: `--continue`/`--resume`으로 재개하면 과거 턴의 훅이 다시 실행되는 게 아니라 저장된 텍스트가 replay된다. 훅이 만든 타임스탬프나 커밋 SHA가 낡은 값으로 돌아온다.
  → **이벤트 시각은 훅이 넣은 값이 아니라 데몬이 수신한 시각을 신뢰한다.** `SessionStart`는 재개 시 `source: "resume"`로 다시 발화하므로 여기서 상태를 재동기화한다.

### 10.2 Cursor (L1)

- **설정 위치**: `.cursor/hooks.json`(프로젝트) · `~/.cursor/hooks.json`(사용자). `"version": 1` 필수
- **이벤트**: `sessionStart`/`sessionEnd`, `preToolUse`/`postToolUse`/`postToolUseFailure`, `beforeShellExecution`/`afterShellExecution`, `beforeMCPExecution`/`afterMCPExecution`, `beforeReadFile`/`afterFileEdit`, `beforeSubmitPrompt`, `preCompact`, `stop`, `subagentStart`/`subagentStop`, Tab용 `beforeTabFileRead`/`afterTabFileEdit`, 앱 레벨 `workspaceOpen`
- **★ `afterFileEdit`이 변경 전/후 내용을 함께 준다** → 파일 감시 없이도 diff를 바로 얻는다. macOS·Windows 감시 문제를 이 경로에서는 우회할 수 있다
- 종료 코드 2 = deny (`permission: "deny"`와 동일)
- HTTP 훅 없음 → 커맨드로 우리 수집기 바이너리를 부른다
- ⚠️ **함정: Cursor는 hooks.json을 시작할 때만 읽는다. 설치 후 Cursor를 재시작하지 않으면 조용히 아무것도 수집되지 않는다.** 설치 명령이 실행 중인 Cursor를 감지해 재시작을 안내해야 한다
- 헤드리스 `cursor-agent` CLI도 동일한 훅을 발화한다

### 10.3 Codex (L1 — 지원 수준 최하)

- **설정 위치**: `.codex/hooks.json` 또는 config.toml 내 `[hooks]` 인라인 테이블. **`codex_hooks = true` 플래그를 켜야 동작한다**
- **이벤트**: `SessionStart`, `UserPromptSubmit`, `Stop` 중심
- `notify`는 **전역에 하나뿐**이다. 이미 설정돼 있으면 덮어쓰지 말고 원래 커맨드를 체이닝해야 한다 (`--chain <원래-실행파일> <원래-인자>`)
- ⚠️ 세션 종료 훅이 없다 → 턴 기반(`Stop`)으로 처리
- ⚠️ 서브에이전트 훅이 없다 → 중첩 세션은 못 잡는다
- ⚠️ **프로젝트 로컬 `.codex/config.toml`은 `notify`, `otel`, `model_provider` 등을 무시한다.** 이 키들은 사용자 레벨 `~/.codex/config.toml`에 써야 한다
- **rollout JSONL**: `${CODEX_HOME:-~/.codex}/sessions/YYYY/MM/DD/rollout-*.jsonl`. `token_count`, `turn_started`, `context_compacted` 이벤트와 `function_call` 항목이 들어 있다

### 10.4 훅이 없는 도구들 (L3)

Copilot, JetBrains AI, 웹 채팅에서 복붙, 데스크톱 앱 등.

- git + 파일시스템 감시만으로 동작한다
- **세션 경계를 알 수 없다** → "변경 발생 후 2초 유휴"를 기준으로 의사(pseudo) 세션을 만든다
- **의도(프롬프트)를 알 수 없다** → EVIDENCE 패널은 비활성화하고, NOW 패널은 파일 변경 타임라인으로 대체한다. RISK · DELTA는 100% 정상 동작한다
- 이 모드에서 화면이 여전히 쓸 만한지가 제품의 하한선이다

### 10.5 OS별 처리: 분기를 하나로 없애는 법

| | Linux | macOS | Windows |
|---|---|---|---|
| 훅 커맨드 실행 셸 | sh | sh | cmd / PowerShell |
| 설정 경로 | `~/.claude/…` | `~/Library/…`, `~/.claude/…` | `%USERPROFILE%\.claude\…` |
| 파일 감시 | inotify | FSEvents | ReadDirectoryChangesW |
| PTY | unix pty | unix pty | ConPTY |

**원칙: 훅 설정에 셸 스크립트를 절대 넣지 않는다.**

- Claude Code → **HTTP 훅**을 기본으로 쓴다. 실행할 셸 자체가 없으므로 OS 분기가 0이다
- Cursor / Codex → 훅 커맨드는 **우리 수집기 단일 바이너리 하나만** 부른다
  ```
  cockpit-collect hook --agent cursor --event afterFileEdit
  ```
  `.sh` 파일을 훅에 넣는 순간 Windows에서 깨진다. 이건 예외 없는 규칙이다
- 수집기는 Go로 만들어 세 OS 정적 바이너리를 크로스 컴파일한다. `chmod +x`, 실행 권한, 셰뱅 문제가 전부 사라진다
- 설정 파일 경로 탐색·백업·병합은 `cockpit install --agent <name>`이 전담한다. 사용자가 JSON을 직접 편집하게 하면 안 된다

### 10.6 호스트 → 컨테이너 전달

```
[호스트]                                    [컨테이너]
Claude Code ── HTTP 훅 ──────────────→ POST 127.0.0.1:7777/events
Cursor/Codex ── cockpit-collect ─────→ POST 127.0.0.1:7777/events
파일 감시 ───── cockpit-collect ─────→ POST 127.0.0.1:7777/events
Claude Code/Codex ── OTLP ───────────→ POST 127.0.0.1:4318/v1/{metrics,logs}
```

- **통신은 호스트 → 컨테이너 단방향이다.** 컨테이너가 호스트를 호출할 일이 없으므로 `host.docker.internal` 유무 차이(리눅스에서 기본 미지원)를 신경 쓸 필요가 없다
- 포트 매핑만 있으면 세 OS 모두 `127.0.0.1`로 도달한다
- **페이로드 상한**: `PostToolUse`의 도구 결과는 수 MB가 될 수 있다. 1MB 초과분은 잘라내고 원본 해시만 저장한다
- **인증**: 최초 기동 시 토큰을 생성해 `.cockpit/token`에 두고, 수집기가 헤더로 보낸다. 로컬 전용이라도 다른 프로세스가 아무 이벤트나 밀어 넣는 걸 막는다. 훅 payload는 신뢰할 입력이 아니다
- **수집기가 죽어 있을 때**: 훅은 실패해도 에이전트를 막으면 안 된다. 타임아웃 2초, 실패 시 조용히 exit 0

### 10.7 워크스페이스(파일) 처리

**원칙: 이벤트는 "무엇이 바뀌었다"만 보내고, 파일 내용은 컨테이너가 마운트에서 직접 읽는다.**

- `/repo`를 읽기 전용으로 바인드 마운트. 훅은 `path` + `hash_before` + `hash_after`만 전송
- 예외: Cursor `afterFileEdit`은 이미 전후 내용을 주므로 그대로 사용한다 (마운트 없이도 diff 가능)
- **폴백 모드**: devcontainer · SSH 원격 · Codespaces처럼 컨테이너가 작업 공간을 못 보는 환경에서는 수집기가 변경 파일 내용을 실어 보낸다. 1MB 초과 파일은 스킵
- **인덱싱 제외**: `.gitignore` + `.cockpitignore` 존중, `node_modules`·`.venv`·`dist`·`build` 제외, 바이너리 판별 후 스킵, 파일당 1MB 상한
- **시크릿**: `.env*`, `*.pem`, `id_rsa` 등은 인덱싱도 로깅도 하지 않는다. 훅 payload에 섞여 들어온 경우도 저장 전에 마스킹
- **인덱스 저장 위치**: `/repo` 안이 아니라 **명명 볼륨**. 읽기 전용 마운트를 유지할 수 있고, macOS 바인드 마운트 I/O 성능 문제도 피한다

### 10.8 토큰 · 비용 추론

가능한 도구가 있고 불가능한 도구가 있다. 되는 것만 정확히 보여준다.

| 도구 | 방법 | 얻는 것 | 정확도 |
|---|---|---|---|
| **Claude Code** | OTLP 네이티브. 컨테이너가 OTLP 수신 엔드포인트가 된다 | 토큰 사용량, 세션, 코드 편집, 도구 호출, 비용 | 공식 계측 — 높음 |
| Claude Code (폴백) | 훅 payload의 `transcript_path` JSONL 파싱 | 턴별 사용량 | 높음, 실시간성은 낮음 |
| **Codex** | rollout JSONL의 `token_count` 이벤트, 또는 `[otel]` `otlp-http` 익스포터 | `total_token_usage`, `last_token_usage`, `cached_input_tokens`, `model_context_window` | 높음 (컨텍스트 잔량까지) |
| **Cursor** | 훅 payload에 토큰 정보 없음 | — | **표시하지 않는다** |
| 훅 없는 도구 | 불가 | — | — |

Claude Code OTLP 설정 (설치 스크립트가 안내):

```bash
export CLAUDE_CODE_ENABLE_TELEMETRY=1
export OTEL_METRICS_EXPORTER=otlp
export OTEL_LOGS_EXPORTER=otlp
export OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
export OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
export OTEL_METRIC_EXPORT_INTERVAL=10000   # 기본 60000 — 화면용으로 단축
export OTEL_LOGS_EXPORT_INTERVAL=5000
```

주의사항:

- **delta temporality로 받아야 한다.** cumulative면 토큰·비용 카운터가 누적값으로 들어와 집계가 크게 부풀려진다
- 기본 export 간격은 메트릭 60초 / 로그 5초다. 실시간 화면에는 너무 느리므로 10초로 낮추도록 안내하되, **이건 사용자 셸 환경변수라 우리가 강제할 수 없다.** 안내 실패를 전제로 UI를 만든다 (토큰 패널은 "지연될 수 있음" 상태를 가진다)
- 컨테이너가 4318 포트도 열어야 한다
- 비용 = 토큰 × 단가인데 단가는 계속 바뀐다. **하드코딩 금지, 요금표는 설정 파일로 분리**

> ⚠️ **원칙: 토큰 표시는 부가 정보다.** 제품의 핵심 주장(§0)은 결정론적 검증이지 비용 추적이 아니다. Cursor처럼 못 얻는 경우 억지 추정치를 띄우면 "모든 판정에 기계적 근거가 있다"는 주장이 흔들린다. **모르면 안 보여준다.**

### 10.9 설치 명령

```
cockpit install --agent claude    # HTTP 훅 등록. 재시작 불필요
cockpit install --agent cursor    # hooks.json 병합 + 재시작 안내
cockpit install --agent codex     # hooks.json + codex_hooks=true + notify 체이닝
cockpit install --telemetry       # OTLP 환경변수 스니펫 출력
cockpit doctor                    # 어느 계층까지 연결됐는지 진단
```

- 기존 설정을 **반드시 병합**한다. 특히 Codex `notify`는 전역 하나뿐이라 덮어쓰면 남의 도구를 망가뜨린다
- 수정 전 백업. `--dry-run`으로 변경될 내용을 먼저 보여준다
- `cockpit doctor`가 중요하다. "훅은 걸렸는데 Cursor를 재시작 안 함", "codex_hooks 플래그 꺼짐", "OTEL 환경변수 미설정" 같은 조용한 실패가 이 제품의 주된 지원 요청이 될 것이다

---

## 11. 방향 결정: 파일 기반이 본체, 훅은 부가

**결론: 판정 엔진은 파일과 git만 본다. 훅 이벤트는 분석기에 절대 들어가지 않는다.**

### 아키텍처 규칙 (위반 금지)

```
분석기 입력 = (워크스페이스 스냅샷, 변경 집합)     ← 파일에서만 온다
훅이 하는 일 = ① 언제 분석할지 트리거  ② 주장·의도 메타데이터 제공
```

훅 payload가 분석기 인자로 들어가는 코드가 한 줄이라도 생기면 아래 다섯 가지를 전부 잃는다.

### 왜 파일 기반인가

1. **제품의 주장이 그렇다.** §0의 주장은 "코드에 대한 기계적 판정"이다. 코드는 파일에 있다. 훅은 코드가 아니라 정황을 준다.
2. **평가가 가능해진다.** 훅에 의존하면 과거 코퍼스로 성능을 측정할 수 없고, "직접 켜서 써보세요"밖에 말할 게 없다. 파일 기반이면 공개 저장소 수십만 커밋에 그대로 돌릴 수 있다 → 논문이 되고, 벤치마크 숫자가 되고, README 첫 줄이 된다. §12가 이 위에 서 있다.
3. **커버리지.** 훅 없는 사용자(Copilot, JetBrains AI, 웹 채팅 복붙)가 전부 살아난다.
4. **CI에 그대로 들어간다.** `cockpit check --exit-code`는 훅과 무관하게 동작해야 한다. 훅 기반 엔진은 CI에 못 들어간다.
5. **종속 리스크가 사라진다.** 훅 스키마가 바뀌어도 판정은 멀쩡하다. 실시간성과 EVIDENCE 패널만 잠시 죽는다.

### 그럼 훅은 왜 필요한가

훅을 빼면 잃는 것은 명확하고, 그건 **부가 가치이지 본체가 아니다.**

| 훅으로만 얻는 것 | 없으면 |
|---|---|
| PR 이전 · 커밋 이전의 실시간성 | 저장 시점 파일 감시로 대체 (약간 늦음) |
| 세션 경계와 프롬프트(의도) | 유휴 기반 의사 세션으로 대체, 의도는 소실 |
| 에이전트의 자연어 주장 | EVIDENCE 패널 비활성 |
| 도구 호출 단위 귀속 | 커밋 단위 귀속으로 하향 |
| 차단(exit 2) | 판정만 하고 못 막음 |

### 3계층과의 대응

§10의 L1/L2/L3는 **수집 계층**의 이야기고, 이 절은 **판정 계층**의 이야기다. 판정 계층은 항상 L3만 쓴다. L1·L2는 화면과 타이밍을 개선할 뿐이다.

> 한 줄 요약: **훅이 0개인 상태로 먼저 완성한다.** 그게 되면 훅은 나중에 얹으면 된다. 순서를 반대로 하면 훅에 맞춰 엔진이 오염된다.

### M0 순서 수정

이 결정에 따라 M0의 첫 작업은 훅 어댑터가 아니다.

1. 파일/git 수집기 + 스냅샷 + 변경 집합 정의
2. 분석기 인터페이스 (입력은 스냅샷과 변경 집합뿐)
3. 그 다음에 Claude Code HTTP 훅 어댑터 (트리거 + 메타데이터로만)

---

## 12. 연구 근거와 논문 가능성

§11의 결정 덕분에 **도구가 곧 실험 장비가 된다.** 파일 기반 엔진은 공개 저장소 코퍼스에 그대로 돌릴 수 있다.

### 12.1 이 제품의 판정을 뒷받침하는 선행 연구

| 우리 분석기 | 근거 연구 |
|---|---|
| `dup.block`, `dup.semantic` | GitClear "Maintainability Gap" (623M 변경, 2023–2026): 중복 +81%, 리팩터링 −70%, 파일 간 재사용 −35%. 단 **저자들도 상관관계라고 명시** |
| `dup.semantic` | AI 에이전트가 사람보다 Type-4 클론을 더 많이 만들지만 리뷰어는 오히려 덜 지적 (arXiv 2601.21276) |
| `test.authenticity` | Hora & Robbes, MSR 2026 (arXiv 2602.00409): 2025년 커밋 120만 건, 저장소 2,168개 분석. 에이전트 커밋의 테스트 커밋 비율 23% vs 비에이전트 13%. mock 커밋 중 9%가 에이전트, 2025년 생성 저장소에서는 19%. 에이전틱 활동이 많은 저장소에서 에이전트 36% vs 비에이전트 28%. **에이전트는 mock 타입 95%로 편중, 사람은 mock 91% · fake 57% · spy 51%로 다양** |
| `test.authenticity` | LLM 생성 테스트의 test smell 연구 (arXiv 2410.10628, TOSEM): Assertion Roulette, Magic Number Test가 반복 출현. TsDetect · JNose가 기존 검출 도구 |
| `claim.verify` | 에이전트 실행 프로버넌스 연구 (arXiv 2606.04990): 로그는 시간순 관찰만 주고, 어떤 근거가 어떤 주장에 영향을 줬는지는 실행 그래프가 필요하다는 논지 |
| 전체 | Microsoft Research 2026: 개발자들이 원한 것은 더 많은 생성이 아니라 **이른 품질 신호, 명확한 권한 경계, 프로버넌스, 불확실성 표시** ("bounded delegation") |

### 12.2 논문 후보 세 개

#### A. 테스트 진위 지표 (가장 유망)

> "에이전트가 만든 테스트는 무엇을 검증하는가: 결정론적 테스트 진위 지표와 뮤테이션 생존율의 상관"

- **빈 곳**: 선행 연구는 test smell 존재 여부와 mock **빈도**까지 왔다. "이 테스트가 실제로 대상을 검증하는가"를 **단일 결정론적 지표**로 조작화한 연구는 아직 없다
- **기여**: (1) mock 대상 == 테스트 대상 판정, assertion-free, 항상 참인 단언, 대응 테스트 부재를 합친 진위 점수 (2) 뮤테이션 생존율을 정답 삼아 지표의 타당성 검증 (3) 에이전트 커밋 대 사람 커밋 비교
- **데이터**: MSR 2026 논문의 방법론(2,168 저장소, 커밋에서 에이전트 판별)을 재사용
- **실행 가능성**: 높음. 우리가 만들 `test.authenticity` 분석기가 그대로 실험 도구

#### B. 결정론적 검출 대 LLM 리뷰

> "AI 생성 코드 결함에 대한 결정론적 검출기와 LLM 리뷰어의 비교 평가"

- **빈 곳**: 2025.07~2026.02 사이 벤치마크 세 개가 같은 과제에 서로 다른 승자를 냈다. 이 불일치 자체가 연구 문제
- **기여**: 재현 가능한 평가 하네스 + "결정론적으로 잡히는 결함 클래스"와 "LLM만 잡는 클래스"의 경계 규명. 후자가 좁다면 그게 이 제품의 정당화
- **리스크**: 상용 도구 평가라 비용·접근·약관 문제. 오픈소스 리뷰어만으로 축소 가능
- 부산물로 §14의 M5 평가 하네스가 나온다

#### C. 유지보수성 신호의 조기 검출력

> "커밋 시점 유지보수성 게이트는 이후 재작업을 예측하는가"

- **빈 곳**: GitClear는 상관만 보여주고 스스로도 인과가 아니라고 밝힌다. 커밋 시점 지표가 **이후 처른·되돌림을 예측하는지**는 검증되지 않았다
- **기여**: 8개 신호를 커밋 시점 계산 가능한 형태로 조작화하고 2주 처른과의 예측력을 측정
- **실행 가능성**: 데이터가 공개 git 이력뿐이라 학부 수준에서 가장 현실적

**추천 순서: A → C → B.** A가 신규성과 실행 가능성이 둘 다 있다. 단 착수 전에 §13의 레드팀과 선행 보강(P0~P5)을 먼저 통과해야 한다.

### 12.3 발표처

- **MSR (Mining Software Repositories)** — 위 참고 논문 상당수가 여기 실린다. Technical Papers 외에 짧은 논문과 도구·데이터 트랙이 있으니 CFP를 확인할 것
- ICSE SEIP, ICSME, SANER (도구 논문 친화적)
- 국내: KCC · KSC. 2024 ICEF 수상 이력이 있으니 국내에서 먼저 한 편 돌리고 국제로 확장하는 경로가 안전하다

### 12.4 순서 주의

**논문 때문에 제품을 비틀지 않는다.** 순서는 도구 → 데이터 → 논문이다. A를 노린다면 `test.authenticity`를 M4가 아니라 M2로 당기는 정도가 적절하고, 그 이상으로 로드맵을 흔들면 둘 다 못 끝낸다.

---

## 13. 논문 A 레드팀: 공격 포인트와 선행 보강

논문 A(§12.2)를 심사자 입장에서 먼저 깬다. **여기서 막히는 구멍은 논문만의 문제가 아니라 분석기 설계의 구멍이기도 하다.** 그래서 이 작업이 M2보다 먼저다.

### 13.1 공격 포인트

| # | 공격 | 위험도 | 방어 |
|---|---|---|---|
| **A1** | **"기존 test smell의 재포장 아닌가?"** assertion 없음 = Empty/Unknown Test, 항상 참 단언 ≈ Redundant Assertion, mock 과다 = Hora & Robbes가 이미 함. TsDetect·JNose가 이미 탐지한다 | **치명** | 신규성을 개별 탐지기에 두지 않는다. (1) **"검증 대상 자체를 mock했는가"는 관계 판정**이다 — 기존 스멜은 테스트 파일 내부만 보고 대상 코드와의 관계를 보지 않는다. 호출 그래프가 필요하다 (2) 기존 연구는 **가독성·유지보수성** 관점, 우리는 **결함 검출력** 관점 (3) 개별 스멜이 아니라 뮤테이션 점수로 타당성이 검증된 **합성 지표** |
| **A2** | **"뮤테이션 점수를 정답으로 쓰는 근거는?"** equivalent mutant는 결정 불가능 문제이고, 연산자 선택에 결과가 좌우된다 | **높음** | 표준 완화법(중복·서브수밍 뮤턴트 제거) 명시 + **보조 정답을 하나 더 둔다**: 알려진 실제 결함을 테스트가 잡는지(Defects4J / BugsInPy 계열). 두 정답이 같은 방향이면 방어가 훨씬 강해진다 |
| **A3** | **"에이전트 커밋을 어떻게 판별했나?"** 로컬에서 Claude Code를 쓰고 본인 이름으로 커밋하면 탐지되지 않는다 → **비에이전트 그룹이 오염된다** | **치명** | (1) Robbes 외, MSR 2026 "Promises, Perils, and (Timely) Heuristics for Mining Coding Agent Activity"의 휴리스틱을 그대로 채택하고 한계를 명시 (2) 오염 방향이 **차이를 과소추정**하는 쪽임을 논증 (3) 표본 200커밋 수동 검증 + 일치도(kappa) 보고 (4) **가장 확실한 방어: RQ를 분리해 지표 타당성만으로도 논문이 서게 만든다** (아래 13.2) |
| **A4** | **"mock이 많은 게 나쁜 테스트가 아니라 외부 의존이 많은 코드인 것 아닌가?"** | **높음** | 가장 아픈 반박이다. 주 지표를 **mock 개수가 아니라 "대상 자체를 mock했는가"라는 관계 판정**으로 잡으면 이 설명으로 해소되지 않는다. 추가로 대상 코드의 외부 의존 수를 공변량으로 회귀에 넣는다 |
| **A5** | "상관을 인과로 말하고 있다" | 중간 | **예측 타당성만 주장한다.** GitClear가 스스로 상관이라고 밝힌 함정을 반복하지 않는다. 문장 단위로 통제 |
| **A6** | "교란 변수 통제가 없다" — 도메인, 테스트 프레임워크, 저장소 나이·규모, 팀 규모, 언어 | 중간 | 공변량 목록을 **분석 전에** 확정하고 문서화 |
| **A7** | "한 언어만 봤다 → 일반화 불가" | 중간 | Python + TypeScript 둘. 선행 연구도 TS/JS/Python이라 비교 가능. mock API가 언어마다 달라(`unittest.mock` vs `jest.mock`) 이식 비용을 명시 |
| **A8** | "표본이 작다" — 선행 연구는 커밋 120만 건 | 중간 | **규모로 경쟁하지 않는다.** 뮤테이션은 계산 비용이 커서 전수가 불가능하다. 층화 표본 설계를 명시적으로 정당화하고, 규모 대신 깊이(정답 두 개, 베이스라인 비교)로 승부 |
| **A9** | "LLM 테스트가 사람 테스트와 겹치는 훈련 데이터 누출" | 낮음 | 선행 연구도 제기한 문제다. 위협 요인으로 서술 |
| **A10** | "지표가 공개되면 회피 학습된다"(Goodhart) | 낮음 | 논의 섹션에 넣으면 오히려 성숙해 보인다. 관계 판정은 회피가 곧 테스트 개선이라는 점이 방어가 된다 |
| **A11** | 재현성 패키지 없음 | 낮음 | MSR에서는 사실상 필수. **우리는 도구가 오픈소스라 오히려 유리하다** |

### 13.2 RQ 재설계 (A3를 무력화하는 구조)

에이전트 판별에 논문 전체를 걸지 않는다. **RQ1·RQ2만으로 논문이 성립하게 만들고, RQ3는 있으면 좋은 것으로 둔다.**

- **RQ1**: 결정론적 테스트 진위 지표는 뮤테이션 생존율을 예측하는가? *(에이전트 판별 불필요)*
- **RQ2**: 그 예측력이 기존 test smell 탐지기 조합보다 나은가? *(에이전트 판별 불필요 — 여기가 기여의 핵심)*
- **RQ3**: 에이전트 저작 커밋의 테스트는 사람 저작보다 진위 점수가 낮은가? *(판별 휴리스틱 의존, 한계 명시)*

RQ3가 무너져도 RQ1·RQ2가 살아 있으면 논문은 선다. 반대로 RQ3에 기대면 A3 하나로 리젝된다.

### 13.3 기술적 실현 가능성 리스크

지표 자체가 구현 가능한지가 먼저다.

| 항목 | 난이도 | 비고 |
|---|---|---|
| assertion 없는 테스트 | 쉬움 | tree-sitter 쿼리 |
| 항상 참인 단언 | 쉬움 | 상수 폴딩 수준 |
| 대응 테스트 부재 | 중간 | 명명 규칙 + import 그래프 |
| **대상 자체를 mock했는가** | **어려움 — 여기가 핵심이자 병목** | 테스트가 무엇을 대상으로 하는지 추론해야 한다. 파일명 규칙(`test_x.py` → `x.py`) + import + 호출 그래프. **동적 언어에서 오탐이 나기 쉽다** |
| 뮤테이션 실행 | 중간 | 계산 비용이 표본 크기를 결정한다 |

> **A4와 A1의 방어가 둘 다 "관계 판정"에 걸려 있는데, 그게 가장 구현이 어렵다.** 이 항목이 안 되면 논문 A는 성립하지 않는다. 그래서 파일럿에서 이것부터 만든다.

### 13.4 선행 보강 작업 (논문 착수 전)

순서대로. **P0에서 결과가 안 나오면 논문 A를 버린다.**

| | 작업 | 기간 | 산출물 |
|---|---|---|---|
| **P0** | **파일럿**: Python 저장소 50~100개, 지표 v0 구현(관계 판정 포함), 뮤테이션 실행, 상관 확인 | 2주 | 상관계수 하나 |
| **P1** | **베이스라인 확보**: 기존 스멜 탐지기를 붙여 같은 표본에서 비교 | 1주 | RQ2의 대조군 |
| **P2** | **에이전트 판별 휴리스틱** 채택 + 200커밋 수동 검증 | 1주 | 정확도·kappa |
| **P3** | **표본 설계 문서화**: 층화 기준, 제외 기준, 필요 표본 크기 | 3일 | 방법론 절 초안 |
| **P4** | **공변량 목록 확정** (분석 전에 고정) | 2일 | — |
| **P5** | **RQ·가설·분석 계획 사전 고정** | 2일 | 사후 짜맞추기 방어 |

### 13.5 중단 기준

- **P0에서 진위 지표와 뮤테이션 생존율 사이에 유의한 상관이 없으면 → 논문 A 중단, C로 전환.**
- **P1에서 기존 스멜 탐지기 조합을 못 이기면 → 기여가 없다. 중단.**
- 관계 판정의 수동 검증 정밀도가 낮으면(예: 0.7 미만) → 지표에서 제외하고 남은 항목으로 축소

### 13.6 제품과의 관계

**P0~P1은 버려지는 작업이 아니다.** 파일럿에서 만드는 지표 v0가 곧 M2의 `test.authenticity` 분석기이고, P1의 베이스라인 비교는 그대로 README의 숫자가 된다. 논문이 중단돼도 제품은 그 산출물을 그대로 쓴다.

반대 방향은 허용하지 않는다. **논문 요구사항 때문에 제품 로드맵을 흔들지 않는다**(§12.4). P0~P5는 총 5~6주이고, 그 이상 늘어나면 제품이 멈춘다.

---

## 14. 저장소 구조 (초안)

```
cockpit/
├── README.md                  # docker run 한 줄이 맨 위에
├── Dockerfile
├── docker-compose.yml
├── core/
│   ├── bus.py                 # 링 버퍼, 구독
│   ├── events.py              # 스키마 + 검증
│   ├── registry.py            # 분석기 등록/스케줄링
│   ├── snapshot.py            # SQLite 스토어
│   └── server.py              # FastAPI + WS
├── collectors/
│   ├── agent/
│   │   ├── base.py            # 어댑터 인터페이스
│   │   └── claude_code.py     # 훅 → 이벤트 정규화
│   ├── git.py
│   ├── fswatch.py
│   └── pty.py
├── analyzers/
│   ├── base.py                # Analyzer Protocol, Finding
│   ├── index/                 # tree-sitter 인덱서, 심볼 그래프
│   ├── dup_block.py
│   ├── error_masking.py
│   └── ...
├── web/                       # React 프런트
├── hooks/
│   └── install.sh             # .claude/settings.json 훅 자동 설치
└── tests/
```

---

## 15. 마일스톤

> ⚠️ **이 절은 대체되었다.** 실제 실행 순서는 `EXECUTION_PLAN.md`(6주, CLI 우선 + 얇은 UI 병행)를 따른다.
> 아래 M0~M4는 CLI 이전에 대시보드부터 만드는 순서라 폐기했다. 장기 비전의 구성 요소 목록으로만 참고한다.


| 단계 | 기간 | 산출물 | 완료 기준 |
|---|---|---|---|
| **M0** | 2주 | **git/파일 수집기 + ChangeSet 정의**, 코어 데몬, 이벤트 스키마, 그다음 Claude Code HTTP 훅 어댑터, `cockpit install`/`doctor`, NOW 패널 | 훅 없이도 변경이 화면에 뜨고, 훅을 켜면 실시간이 된다 |
| **M1** | 3주 | tree-sitter 인덱서, 심볼 그래프, DELTA 패널 | 중형 레포 첫 인덱싱 ≤10초, 증분 갱신 ≤100ms |
| **M2** | 3주 | `dup.block`, `risk.error-masking`, `test.authenticity`(§12.4로 앞당김), RISK 패널 | 본인 레포에서 실제 문제를 잡아낸다 |
| **M3** | 2주 | 터미널, 레이아웃 확정, **도커 이미지(멀티아치), 호스트 수집기, README** | Linux · macOS · Windows 세 곳에서 실제로 뜬다 → **여기서 공개** |
| **M4** | 3주 | EVIDENCE 패널, `claim.verify`, 리뷰 패킷 내보내기 | 기존 검증기 프로젝트 흡수 완료 |

**M3에서 공개한다.** M4를 기다리면 늦다.

### 성능 목표

- 첫 인덱싱: 10만 줄 레포 기준 ≤10초
- 증분 갱신: 파일 1개 변경 시 ≤100ms
- 이벤트 → 화면 반영: ≤200ms
- 메모리: 상주 ≤500MB
- 도커 이미지: ≤300MB

---

## 16. 리스크와 검증

### R1. 두 번째 모니터를 정말 내줄 것인가 (최대 리스크)
조사로 답이 안 나온다. **M1 종료 후 2주간 본인이 실제 개발에 켜놓고 하루 몇 번 봤는지 센다. 하루 3회 미만이면 패널 구성이 틀린 것.**

### R2. 에이전트 훅 스키마 종속
Claude Code 훅 이벤트는 30개 규모로 계속 확장 중이고, Cursor·Codex도 마찬가지다. 공급자가 바꾸면 그날 깨진다. M0에 어댑터 경계 + 스키마 버전 태그(`v` 필드)를 넣고, 알 수 없는 이벤트는 버리지 말고 `unknown` 타입으로 통과시킨다.
조용한 실패가 더 무섭다 — Cursor 미재시작, `codex_hooks` 플래그 누락, OTEL 환경변수 미설정. `cockpit doctor`를 M0에 함께 넣는 이유다.

### R3. 노이즈
3등급 판정 + 보수적 기본값 + 근거 필수. 오탐 1건이 신뢰를 크게 깎는다.

### R5. 크로스 플랫폼 검증 비용
세 OS를 지원한다는 건 개발 시간이 아니라 **디버깅 시간**이 늘어난다는 뜻이다. 실기 확인이 어려우면 M3에 GitHub Actions 매트릭스(ubuntu / macos / windows)로 스모크 테스트라도 걸어둔다.

### R4. "얕은 버전 6개" 함정
패널을 늘리는 것보다 RISK를 깊게 파는 데 시간을 쓴다. 패널 추가 요청이 와도 M4까지는 거절.

---

## 17. 시장 근거 (설계 판단의 출처)

- 코드 리뷰 시간 중앙값 +441.5%, 작업 처리량 +33.7% — Faros AI, 개발자 22,000명 텔레메트리, 2026
- AI 보조 PR은 약 2.5배 크고 리뷰어를 약 5배 오래 대기, 에이전트 PR 픽업 타임 5.3배 — LinearB, PR 810만 건, 2026
- 리팩터링 −70%, 중복 +81%, 복붙 +41%, 에러 마스킹 catch +47%, 파일 간 재사용 −35% — GitClear "Maintainability Gap", 코드 변경 6.23억 건, 2026
- 커밋 코드의 42%가 AI 산출, 96%가 기능적 정확성을 완전히 신뢰하지 않음 — Sonar State of Code, 개발자 1,100명+, 2026
- AI 에이전트가 사람보다 Type-4 클론을 더 많이 생성하나 리뷰어는 덜 지적 — arXiv 2601.21276
- 경쟁 도구 가격대: Sourcery $12 ~ Graphite Team $40 / 개발자 / 월. 전부 LLM 기반, PR 시점 동작

---

## 18. 열린 결정 사항

- [ ] 프로젝트 이름 (COCKPIT은 가칭)
- [ ] 라이선스: MIT vs Apache-2.0
- [ ] M0 이벤트 스키마 필드 최종 확정
- [ ] 첫 지원 언어를 Python으로 할지 TypeScript로 할지
- [ ] CI 모드(`cockpit check --exit-code`)를 M3에 넣을지 M5로 미룰지
- [ ] 호스트 수집기 언어: Go vs Rust
- [ ] 호스트 셸 터미널 연결 시점 (M5 이후로 미루는 게 기본안)
- [ ] Cursor/Codex 훅에서 수집기 바이너리 배포 경로 (PATH 등록 vs 절대경로 기입)
- [ ] L3(훅 없음) 모드에서 의사 세션 경계 판정 기준 (유휴 2초가 적절한지)
- [ ] 논문 A를 실제로 진행할지, 진행한다면 국내 학회 먼저인지 MSR 직행인지
- [ ] `test.authenticity`를 M2로 앞당길 때 첫 대상 언어 (Python이 뮤테이션 도구 생태계상 유리)
