# 실행 계획 (6주) — CLI 본체 + 얇은 UI 병행

> `PROJECT_BRIEF.md`가 비전이라면 이 문서는 실행이다.
> 충돌하면 **이 문서가 이긴다.**

작성일: 2026-09-09
전제: 솔로 개발, 주당 실작업 20~25시간 가정

---

## 0. 세 가지 규칙

### 규칙 1 — JSON이 유일한 접점

```
cockpit check --json   ←  이것만이 계약이다
      │
      ├─ watch 모드      : 같은 JSON을 반복 방출
      ├─ 웹 뷰           : 그 JSON을 받아 그림
      ├─ Claude Code 훅  : "다시 검사해"라는 트리거일 뿐
      └─ 대시보드        : 웹 뷰가 자란 것
```

**UI 계열에는 분석 로직이 한 줄도 없다.** 정렬·필터·색칠만 한다.
UI가 JSON에 없는 값을 필요로 하면 → **UI를 고치지 말고 JSON 스키마를 고친다.**

### 규칙 2 — Track B는 Track A를 절대 막지 않는다

Track B(UI 계열)는 매주 **하루**만 쓴다. 그날 안에 안 끝나면 그 주는 거기서 멈추고 다음 주로 넘긴다. 밀린 것을 따라잡으려 Track A를 미루지 않는다.

### 규칙 3 — 빌드 도구 금지

웹 뷰에 번들러·프레임워크·npm 스크립트를 넣지 않는다. **정적 HTML 한 장 + CDN**으로 시작한다. 번들러를 넣는 순간 그 자체가 워크스트림이 된다. React가 필요해지는 시점은 대시보드가 실제로 커진 뒤이고, 그때 옮기면 된다.

---

## 1. 트랙 구조

| | Track A (본체) | Track B (껍데기) |
|---|---|---|
| 목적 | 판정 엔진의 쓸모 증명 | 실사용 감각 확보 + 도그푸딩 |
| 시간 배분 | 주 4일 | 주 1일 |
| 실패 시 | 프로젝트 중단 | 그냥 다음 주로 미룸 |
| 산출물 | `cockpit` CLI | `cockpit watch` / `serve` / 훅 |

---

## 2. 주차별 계획

| 주 | Track A (4일) | Track B (1일) |
|---|---|---|
| **1** | 저장소 스캐너, `ChangeSet`, tree-sitter 인덱서, `Finding` 스키마 확정 | — (JSON 스키마 확정에 집중) |
| **2** | 분석기 3개: `risk.error-masking`, `dup.block`, `test.assertion-free`. 터미널 표 + `--json` 출력 | **watch 모드** — 파일 감시 + 디바운스 + 재실행 |
| **3** | **겹침 측정 + 라벨링** (아래 §4) | **웹 뷰** — 정적 HTML 한 장, watch가 WebSocket으로 JSON push |
| **4** | 라벨링 결과로 임계값 조정, `--exit-code`, 설정 파일 | **GitHub Action** + PR 코멘트 |
| **5** | P0 파일럿: 관계 판정(대상 자체를 mock했는가) 구현 | **Claude Code HTTP 훅** — 트리거로만 |
| **6** | P0 파일럿: 뮤테이션 상관 측정 | **대시보드** — 웹 뷰를 2~3 패널로 확장 |

6주 끝 상태:
- 어떤 레포에서든 도는 CLI
- 오탐률 실측치와 기존 린터 대비 고유 가치 수치
- PR에 코멘트를 다는 GitHub Action
- 세컨드 모니터에 띄울 수 있는 실시간 화면
- 논문 A의 go/no-go 답

---

## 3. JSON 계약 (1주차에 확정)

```jsonc
{
  "schema": 1,
  "repo": "/abs/path",
  "generated_at": "2026-09-09T12:00:00Z",
  "scope": { "kind": "diff", "base": "HEAD~1", "head": "working" },
  "summary": { "block": 0, "warn": 3, "info": 12, "files_scanned": 214 },
  "findings": [
    {
      "id": "dup.block",
      "severity": "warn",
      "file": "src/auth/token.py",
      "span": [42, 57],
      "symbol": "verify_token",
      "message": "5줄 이상 중복 블록",
      "evidence": {
        "hash": "a3f1…",
        "matches": [
          { "file": "src/user/session.py", "span": [88, 103] }
        ]
      }
    }
  ]
}
```

- `schema` 필드는 1주차부터 넣는다. 나중에 넣으면 못 넣는다
- `evidence`는 선택 항목이 아니다. 이게 없는 finding은 만들지 않는다 (§0 원칙)
- watch 모드는 같은 객체를 NDJSON으로 흘린다. 별도 포맷을 만들지 않는다

---

## 4. 3주차가 진짜 게이트다

Track A 3주차는 코드를 거의 안 쓴다. 대신 이걸 한다.

1. 공개 저장소 100개에 `cockpit check` 실행
2. 같은 저장소에 ruff(`BLE001`, `S110`, `E722`), pylint, SonarQube CE 실행
3. **겹침 비율 계산** — 우리 findings 중 몇 %가 이미 무료로 잡히는가
4. 우리만 잡는 findings 100개를 **직접 라벨링** (진짜 문제 / 오탐)

### 판정 기준

| 결과 | 조치 |
|---|---|
| 고유 findings 비율이 낮고 오탐률도 높음 | **분석기 구성을 다시 짠다.** 4주차 계획 폐기 |
| 고유하지만 오탐률이 높음 | 임계값·판정 규칙 재설계에 4주차를 전부 쓴다 |
| 고유하고 오탐률도 낮음 | 계획대로 진행. 이 숫자가 README 첫 줄이 된다 |

지루하지만 이 작업이 제품의 운명을 결정한다. **감으로 정한 임계값은 반드시 시끄럽고, 시끄러운 도구는 일주일이면 꺼진다.**

---

## 5. Track B 각 항목의 예산과 완료 기준

| 항목 | 예산 | 완료 기준 | 넘어가면 |
|---|---|---|---|
| watch 모드 | 1일 | 파일 저장 후 1초 내 결과 갱신 | 폴링으로 낮춰서 끝낸다 |
| 웹 뷰 | 1일 | 브라우저에서 findings 목록과 evidence를 본다 | 표 하나로 축소 |
| GitHub Action | 1일 | PR에 코멘트가 달린다 | 체크만 남기고 코멘트 포기 |
| Claude Code 훅 | 1일 | 에이전트가 파일을 고치면 자동 재검사 | HTTP 훅 안 되면 watch로 대체 (이미 됨) |
| 대시보드 | 1일 | 패널 2~3개 | 웹 뷰 그대로 둔다 |

**훅이 마지막에서 두 번째인 이유**: watch 모드가 이미 동작하면 훅은 지연을 200ms 줄여주는 최적화일 뿐이다. 없어도 제품이 성립한다. 순서를 앞당길 이유가 없다.

---

## 6. 이번 6주에 하지 않는 것

- ❌ 도커 이미지 (CLI가 자리 잡은 뒤. `pipx install`이 기본)
- ❌ 터미널 패널
- ❌ Go 수집기
- ❌ 플러그인 공개 API
- ❌ MinHash/LSH, Tarjan, 인터벌 트리 — **측정해서 느린 게 확인된 다음에** 꺼낸다
- ❌ Python 외 언어
- ❌ Cursor · Codex 어댑터 (Claude Code 훅 하나로 패턴 검증 후)
- ❌ 논문 집필 (P0 결과만 확보)

전부 §14 이후의 후보이지 폐기가 아니다. **다만 지금 손대면 6주가 안 끝난다.**

---

## 7. 성능 원칙

목표 수치는 하나만 둔다: **중형 저장소 전체 검사 10초 이내, 증분 1초 이내.**

이걸 못 지킬 때만 최적화한다. 순서는:

1. 측정 (어느 분석기가 몇 초 쓰는지)
2. 캐시 (파일 해시 기반 결과 재사용)
3. 병렬 (프로세스 풀)
4. **그 다음에** 알고리즘 교체

4번을 1번보다 먼저 하면 안 된다. 그게 이 프로젝트의 가장 흔한 실패 경로다.

---

## 8. 6주 후 분기

| 3주차 게이트 | 5~6주차 P0 | 다음 6주 |
|---|---|---|
| 통과 | 상관 확인됨 | 논문 A 준비 + 언어 확장 + 도커 |
| 통과 | 상관 없음 | 논문 A 폐기, 제품에 집중. 대시보드·Cursor 어댑터 |
| 실패 | — | 분석기 재설계. UI는 그대로 두고 엔진만 갈아엎는다 |

**엔진과 껍데기를 분리해둔 덕에, 셋 중 어느 경로로 가도 버리는 코드가 최소다.** 이게 규칙 1의 진짜 이유다.

---

## 9. 지금 정해야 하는 내부 구조 결정

알고리즘 선택보다 이게 먼저다. 아래 여섯 개는 **나중에 바꾸려면 전부 갈아엎어야 하는 것들**이다.

### 9.1 캐시 키 — 파일 해시만으로는 틀린다

```
cache_key = hash(파일 내용 정규화) + analyzer_id + analyzer_version
          + grammar_version + config_hash
```

분석기 로직을 고쳤는데 파일 해시가 같아서 옛 결과가 살아 있으면, **조용히 틀린 판정**이 나온다. 결정론을 주장하는 제품에서 이건 치명적이다.

- `analyzer_version`은 `Finding`에도 넣는다. 어느 버전이 만든 판정인지 나중에 반드시 필요해진다
- tree-sitter 문법 버전을 lock한다. 문법이 바뀌면 쿼리 결과가 바뀌고 판정이 바뀐다

### 9.2 크로스파일 분석은 파일 단위 캐시가 불가능하다

`dup.block`은 A를 고치면 **건드리지도 않은 B의 판정이 바뀐다.** 파일 단위로 결과를 캐시하면 틀린다.

```
캐시하는 것:  파일 → 지문 목록        (파일 단위, 캐시 가능)
캐시 못 하는 것: 지문 → 파일 목록      (역인덱스, 메모리에 유지하고 증분 갱신)
```

지문 추출만 캐시하고 **매칭은 매번 인메모리 역인덱스에서** 한다. 파일 하나가 바뀌면 그 파일의 옛 지문을 역인덱스에서 빼고 새 지문을 넣는다. 이게 증분 1초를 만드는 실제 구조다.

### 9.3 Finding에 안정적인 id를 준다

줄 번호를 id로 쓰면 위에 한 줄만 추가돼도 전부 새 finding이 된다.

```
finding_id = hash(analyzer_id + 심볼 경로 + 매칭된 내용 해시)
줄 번호는 표시용일 뿐 신원이 아니다
```

이걸 안 하면 아래 세 가지가 전부 불가능해진다.

- watch/웹 뷰에서 **델타 전송** (매번 전체 JSON을 push하면 findings 수천 개에서 브라우저가 죽는다)
- **baseline** — 기존 레포에 처음 돌리면 findings가 수천 개다. 현재 상태를 승인된 것으로 스냅샷하고 신규만 보여주지 못하면 아무도 CI에 안 넣는다
- **suppress** — "이건 무시" 표시가 다음 커밋에서 풀려버린다

### 9.4 출력 순서를 결정론으로 고정한다

파일 순회 순서, 병렬 실행 완료 순서, 딕셔너리 순회는 실행마다 다르다. 같은 입력에 findings 순서가 달라지면 **"결정론적"이라는 제품의 주장 자체가 흔들린다.**

정렬 규칙을 스펙에 박는다: `파일 경로 → 시작 줄 → analyzer_id → finding_id`. 병렬 처리 결과는 반드시 정렬 후 출력한다.

### 9.5 AST를 들고 있지 않는다

```
파싱 → 필요한 사실만 추출(심볼, 지문, findings) → AST 폐기
```

AST를 LRU에 캐시하겠다는 발상은 메모리 상한을 즉시 깬다. 캐시할 것은 AST가 아니라 **추출 결과**다.

### 9.6 asyncio와 CPU 작업을 섞지 않는다

FastAPI 이벤트 루프에서 분석 함수를 그냥 호출하면 분석이 도는 동안 WebSocket이 멈춘다. 화면이 얼어붙는 가장 흔한 원인이다.

```
이벤트 루프  : 웹소켓, HTTP만
워커         : 분석 (별도 프로세스/스레드)
연결         : 큐 + 세대 카운터
```

**취소는 세대 카운터로 한다.** watch 중 이전 분석이 안 끝났는데 새 변경이 오면, 파이썬에서 작업을 진짜로 죽이기는 까다롭다. 대신 요청마다 세대 번호를 붙이고 **결과가 돌아왔을 때 세대가 낡았으면 버린다.** 훨씬 단순하고 충분하다.

---

## 10. 성능 실전 항목 (측정 후 적용)

§7의 순서(측정 → 캐시 → 병렬 → 알고리즘)를 지키되, 아래는 거의 항상 이기는 수라 먼저 알고 있어도 된다.

| 항목 | 내용 |
|---|---|
| **순회는 `git ls-files`로** | `os.walk`로 `node_modules`·`.venv`를 들어갔다 나오는 것만으로 macOS에서 수 초가 나간다. git 저장소면 `git ls-files`가 순회와 `.gitignore` 처리를 동시에 공짜로 해준다. 비 git 저장소일 때만 walk |
| **순회를 파이썬 루프로 하지 말 것** | py-tree-sitter에서 노드를 파이썬으로 하나씩 돌면 그게 병목이다. **tree-sitter 쿼리로 밀면 C에서 실행된다.** 같은 판정도 10배 차이가 난다 |
| **쿼리는 코드가 아니라 데이터로** | 언어별 `.scm` 쿼리 파일로 분리. 그래야 언어 추가가 파일 추가로 끝난다 |
| **병렬은 임계값을 둔다** | 프로세스 풀 시작 비용은 크고, Windows·macOS는 fork가 아니라 spawn이라 워커가 모듈을 다시 import한다(문법 로딩이 워커마다 반복). 20파일 미만이면 단일 프로세스가 빠르다 |
| **SQLite는 복원용** | WAL 모드, `synchronous=NORMAL`, 파일마다 커밋하지 말고 배치 트랜잭션. 상태 대부분은 메모리에 두고 DB는 재시작 복원과 baseline 저장에만 쓴다 |
| **파일당 상한** | minified JS, 생성 코드, 1만 줄 파일이 파서를 오래 붙잡는다. 크기·시간 상한을 두고 스킵 사유를 기록 |

---

## 11. watch 모드의 함정

에이전트는 파일을 사람처럼 저장하지 않는다.

- **연속 쓰기**: trailing 디바운스(조용해질 때까지 대기) + 최대 대기 상한. 단순 고정 지연은 계속 밀린다
- **부분 저장 상태 파싱**: 저장 중 파일을 읽으면 파스 에러가 난다. **에러를 findings로 올리지 말고 무시 후 재시도.** 화면에 빨간 게 번쩍이는 순간 신뢰를 잃는다
- **에디터 임시 파일**: `.swp`, `~`, `.#`. vim·VSCode는 임시 파일에 쓰고 rename하므로 inotify에는 CREATE/MOVED_TO로 들어온다. 이름 필터가 필요하다
- **인코딩**: UTF-8이 아닌 파일과 BOM. 해시 전에 CRLF와 함께 BOM도 제거한다 (BRIEF §9 지뢰 2와 같은 이유)
